name: Deploy Worker

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Publish Cloudflare Worker
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: ${{ secrets.CF_ACCOUNT_ID }}
          workingDirectory: .
          command: publish cloudflare-worker.js

      - name: Set Worker secrets
        run: |
          echo "${{ secrets.CEREBRAS_API_KEY }}" | wrangler secret put CEREBRAS_API_KEY
          echo "${{ secrets.GROQ_API_KEY }}" | wrangler secret put GROQ_API_KEY
          echo "${{ secrets.CHAT_STATUS }}" | wrangler secret put CHAT_STATUS